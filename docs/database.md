# Database

This project has no database — it's a PowerShell/Python installer that
copies files and merges JSON/Markdown into destination projects; there is
no persistent store of its own. This file is intentionally left minimal —
populate it if a database is ever introduced.

*Last updated: 2026-07-07 — verified against commit `9ea2b62`.*
