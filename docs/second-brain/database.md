# Database

This project has no database — it's a Claude Code plugin plus a git-bash
bootstrap that scaffolds files into destination projects; there is no
persistent store of its own. This file is intentionally left minimal —
populate it if a database is ever introduced.

*Last updated: 2026-07-08 — verified against commit `b595503`.*
